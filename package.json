{
  "name": "your-app-name",
  "version": "1.0.0",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  },
  "dependencies": {
    "eslint": "9.8.0",
    "jest": "29.7.0"
  }
}
